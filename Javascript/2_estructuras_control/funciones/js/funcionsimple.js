/** 
 * 
 * Función: encapsular bajo un mismo nombre todo el codigo que realice una accion concreta
 * 
 */

// Definicion de la funcion
// que hace la funcion
function saludar (){
    // sentencias que forman parte de la funcion
    console.log('Hola mundo!');
}

// Ejecucion de la funcion
//LANCE LAS SENTENCIAS QUE FORMAN PARTE DE DICHA FUNCION
saludar();
saludar();
saludar();

for (let i = o; i < 100; i++) {
    saludar();
}
